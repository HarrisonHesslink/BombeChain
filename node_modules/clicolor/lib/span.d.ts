import { Pen } from "./pen";
export declare type Spans = Array<Span | string>;
export declare class Span {
    pen: Pen;
    spans: Spans;
    length: number;
    constructor(pen: Pen, spans: Spans);
    slice(start?: number, end?: number): Span;
    private render(buffer, active, currentPen, emitCodes);
    toString(emitCodes?: boolean): string;
}
export declare function span(pen: Pen, ...spans: Spans): Span;
