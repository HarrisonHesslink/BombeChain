export declare type Options = {
    frequency?: number;
    width?: number;
};
export declare class StatusUpdater {
    private frequency;
    private width;
    lastUpdate: number;
    private currentMessage;
    private timer?;
    private blankLine;
    constructor(options?: Options);
    private render(message);
    update(message?: string): string;
    clear(): string;
}
export declare function spaces(n: number): string;
