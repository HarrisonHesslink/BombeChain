export declare const RESET_ATTRIBUTES: string;
export declare const TRANSPARENT: number;
export declare class Canvas {
    width: number;
    height: number;
    private grid;
    private x;
    private y;
    private fg;
    private bg;
    constructor(width: number, height: number);
    color(c: string | number): this;
    backgroundColor(c: string | number): this;
    at(x: number, y: number): this;
    write(s: string): this;
    clear(): this;
    scroll(deltaX: number, deltaY: number): this;
    fillBackground(color: string | number): this;
    toStrings(options?: {
        dropBlanks?: boolean;
    }): string[];
    private _put(x, y, bg, fg, ch);
    private _puti(index, bg, fg, ch);
    private _get(x, y);
    private _geti(index);
}
